import Section from '@/components/emailComponents/Section';

const EmailSection = () => {
  return (
    <Section classes="text-center" bgColor="#7dd3fc">
      This is a React Component Section.
    </Section>
  );
};

export default EmailSection;
