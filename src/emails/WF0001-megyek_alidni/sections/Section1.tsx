import * as Email from '@/components/emailComponents';

const EmailSection = () => {
  return (
    <Email.Section className="text-14 text-center">
      You can create a section just like this. Could be useful for an intro.<br />
    </Email.Section>
  );
};

export default EmailSection;